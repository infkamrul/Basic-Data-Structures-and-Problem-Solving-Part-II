#include <bits/stdc++.h>

using namespace std;
int main()
{
    string st="kamrulIslam";

    for(auto it=st.begin(); it!=st.end(); it++)
    {
        cout<<*it<<endl;
    }
    cout<<endl;
    for(auto it=st.rbegin(); it!=st.rend(); it++)
    {
        cout<<*it<<endl;
    }
}
